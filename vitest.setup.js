// Importing custom matchers from the @testing-library/jest-dom library
// These matchers extend Jest's assertions to include DOM-specific assertions
// such as checking if an element is visible, contains specific text, etc
import '@testing-library/jest-dom';
